#ifndef EVENT_HPP
#define EVENT_HPP

#include <string>

class Event {
    private:
        
    public:
        std::string description;
        std::string date_range;
        std::string location;
        std::string organizer;
};

#endif