#ifndef COMMANDS_HPP
#define COMMANDS_HPP

#include <string>
#include <vector>
using namespace std;

class Commands {
    public:
        logIn(string username, string password);
        signUp(string username, string password);

};

#endif